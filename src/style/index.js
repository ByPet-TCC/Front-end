import { StyleSheet } from "react-native";

const IndexStyle =  StyleSheet.create({
    container: {
        fontFamily: 'Poppins'
    },

    ImageBackground: {
        height: '100%'
    }
})

export default IndexStyle;